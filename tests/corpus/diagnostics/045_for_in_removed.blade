// TEST: Diag Imperative For In Removed (diag)
// ERROR: BL1003 @ 13:5
// ERROR-CONTAINS: The imperative `for x in a..b { ... }` statement has been removed
// The imperative `for IDENT in a..b { ... }` statement was removed from the
// surface language (recursive-arrays arc): iteration is loop objects for
// parallel structure and recursive arrays for sequential structure. The parser
// keeps the `for`-disambiguation shell (Parser.fs parseBlock, KwFor arm) so
// this shape gets a STEERING diagnostic instead of a confusing misparse at
// `in` — that steer is what this file pins. BLOCK position is what this file
// exercises; top level has its OWN copy (parseDecl, sharing the one
// `forInRemovedMsg`), needed now that a bare top-level expression parses.
function f() -> Int64 = {
    let mut s = 0
    for k in 0..3 {
        s += k
    }
    s
}
