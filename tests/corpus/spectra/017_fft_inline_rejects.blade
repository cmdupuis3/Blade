// TEST: Spectra fft: inline array argument steers to annotated let (rejects)
// ERROR: BL5400
// Spectra ops read the DECLARED shape: the argument must be a plain
// variable naming a module-level let with a full Array annotation.
import spectra as sp
let X = sp.fft([1.0, 2.0, 3.0, 4.0])
