// TEST: Math gating: selective import steers to qualified (rejects)
// `from math import ...` would reintroduce the global names the module
// system removes; the elaborator rejects it with steering toward
// `import math [as <alias>]`.
from math import svd
let r = 1.0
