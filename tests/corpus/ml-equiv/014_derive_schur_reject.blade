// TEST: ML Equiv Derive Schur Reject (rejects)
import ml as ml
// ============================================================================
// derive_linear between specs sharing no (l, parity): hom_dim = 0 — the
// complete basis is empty, so the request is refused (BL4007) rather than
// synthesizing a constant-zero layer.
// ============================================================================
let static SIN = [(0, 0, 1)]
let static SOUT = [(1, 1, 1)]
let bad = ml.derive_linear(SIN, SOUT, [0.0], [1.0])
