// TEST: ML Galilean Nonparam Rejected (rejects)
import ml as ml
// The galilean conjunct names boost-variant PARAMETERS; a name that is not
// a parameter of the function is BL4009 at the declaration.
function bad(u: Float, v: Float) where ml.galilean(zz) -> Float =
  u - v
