// TEST: AntisymIdx Parse Type
// Parse AntisymIdx in a function signature, with block body
function skew(A: Array<Float64 like Idx<n>>) -> Float64 = {
    let L = method_for(A, A)
    let f = lambda(x, y) where comm(x, y) -> x - y
    0.0
}
