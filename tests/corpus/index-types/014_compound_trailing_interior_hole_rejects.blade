// TEST: Compound Trailing Interior Hole (rejects)
// ERROR: BL4003
// B((0, 0), _, 1) fails at the FIRST hurdle now: the tuple spelling itself
// rejects on a compound axis (flat-subscript conversion) before the interior
// trailing hole is even considered. (Historically this was the BL3999
// interior-trailing-hole restructure rejection.)
type Lat = Idx<2>
type Lon = Idx<3>
type T = Idx<2>
let dense: Array<Float64 like Lat, Lon, T> = [
    [[0.0, 1.0], [10.0, 11.0], [20.0, 21.0]],
    [[30.0, 31.0], [40.0, 41.0], [50.0, 51.0]]]
let m: Array<Bool like Lat, Lon> = [[true, false, true], [true, true, false]]
let B = compound(dense, m)
let x = B((0, 0), _, 1)
// EXPECT: typecheck failure - the tuple spelling (and wildcards) moved to SparseIdx / flat subscripts.
