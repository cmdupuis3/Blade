// TEST: Struct Constraint Invalid (aborts)
// Struct with where constraint - INVALID construction (should abort)
// ABORT: Constraint violation in Balanced
struct Balanced {
    a: Int,
    b: Int,
    total: Int
} where a + b == total
let x = Balanced { a = 3, b = 7, total = 99 }
