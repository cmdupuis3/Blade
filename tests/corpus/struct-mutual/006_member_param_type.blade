// TEST: Mutual Member Param Type (rejects)
// Member types cannot appear in function parameter positions.
struct Particle {
    pos: Float,
    vel: Float,
    mass: Float
}
type P1 = Particle
and P2 = Particle
where P1.vel + P2.vel == 0.0
function speed(p: P1) -> Float = p.vel
