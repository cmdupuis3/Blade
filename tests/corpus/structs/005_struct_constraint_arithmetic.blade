// TEST: Struct Constraint Arithmetic
// Struct with arithmetic constraint - valid
struct Ratio {
    num: Float64,
    den: Float64
} where den != 0.0
let r = Ratio { num = 3.14, den = 2.0 }
let half = r.num / r.den
// EXPECT: half = 1.57
