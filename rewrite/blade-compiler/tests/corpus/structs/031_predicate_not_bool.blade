// TEST: Struct Predicate Not Bool (rejects)
// A where-conjunct calling an Int-returning function is a type error.
static function total_of(a: Int, b: Int) -> Int = a + b
struct Bad {
    a: Int,
    b: Int
} where total_of(a, b)
let x = Bad { a = 1, b = 2 }
