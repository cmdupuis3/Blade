// TEST: Mutual Nonstatic Ident In Where (rejects)
// Free identifiers in a mutual constraint must be statically evaluable.
let runtime_val = 1.0
type V1 = Float
and V2 = Float
where V1 + V2 == runtime_val
let (u, w): (V1, V2) = (0.5, 0.5)
