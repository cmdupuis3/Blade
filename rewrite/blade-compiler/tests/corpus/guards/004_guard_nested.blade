// TEST: Guard Nested
// Nested match with guards (multi-line match body requires braces)
let x = 10
let y = 20
let outer = match x with
    | n if n > 5 -> {
        match y with
        | m if m > 15 -> n + m
        | _ -> n
    }
    | _ -> 0
// EXPECT: outer = 30
